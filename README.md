# gmail-api-campaign-sender
Python tool that sends personalized HTML email campaigns from a CSV contact list using Gmail API with OAuth2. Features PDF attachments per contact, variable substitution in templates, idempotent send log, and dry-run mode. Zero passwords stored — production-ready outreach automation.
